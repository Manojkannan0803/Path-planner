function params = create_ip_params()

params.thetad = []; % Initialize orientation angle
params.gammad = []; % Initialize articulation angle
params.motion_primitive_folder = []; % Folder for motion primitives
params.straight_lengths = []; % Initialize straight primitive lengths
params.num_samples = 0; % Number of samples for interpolation